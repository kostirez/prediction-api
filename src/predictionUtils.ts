import { PredictionRequest, PredictionResult } from "./predictionTypes";

export function predictFirst(inputData: PredictionRequest): PredictionResult {
  const predictions: PredictionResult = {
    predictions: []
  }
  return predictions;
}

export function predictSecond(inputData: PredictionRequest): PredictionResult {
  const predictions: PredictionResult = {
    predictions: []
  }
  return predictions;
}

export function predictThird(inputData: PredictionRequest): PredictionResult {
  const predictions: PredictionResult = {
    predictions: []
  }
  return predictions;
}
